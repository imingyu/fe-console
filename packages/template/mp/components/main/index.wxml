<view class="fc-hand {{visable?'is-hide':''}}" hover-class="fc-active" <%= bindEvent('tap') %>="toggleVisable">Fc</view>
<view wx:if="{{visable || mounted}}" class="cid-{{$cid}} tid-{{$tid}} fc-modal {{isFullScreenPhone?'fc-adap-scroll':''}} {{visable?'is-show':''}} {{fullScreen?'is-full':''}}">
    <view class="fc-modal-body">
        <fc-tabs tabs="{{tabs}}" active="{{activeTabIndex}}" outerClass="fc-product-tabs" parentCid="{{ $cid }}" parentTid="{{ $tid }}" tidAlias="FcProductTypeTabs">
            <view slot="headBefore" class="fc-tabs-action" <%= bindEvent('tap') %>="toggleZoom">
                <view class="fc-tabs-zoom fc-ic-{{fullScreen?'compress':'expand'}}">
                    <view class="fc-ic-arrow-1"></view>
                    <view class="fc-ic-arrow-2"></view>
                </view>
            </view>
            <view slot="headAfter" class="fc-tabs-action fc-tabs-action-right" <%= bindEvent('tap') %>="close">
                <view class="fc-tabs-close fc-ic-close"></view>
            </view>
            <view slot="panel-all">功能开发中</view>
            <view slot="panel-console">功能开发中</view>
            <fc-api-reader slot="panel-api" class="fc-tab-panel-reader" active="{{activeTabIndex===2}}" parentCid="{{ $cid }}" parentTid="{{ $tid }}"></fc-api-reader>
            <view slot="panel-view">功能开发中</view>
            <view slot="panel-event">功能开发中</view>
            <view slot="panel-storage">功能开发中</view>
            <view slot="panel-system">功能开发中</view>
        </fc-tabs>
    </view>
    <view wx:if="{{isFullScreenPhone}}" class="fc-modal-adap">
        <view class="fc-ad">Github@imingyu/fe-console</view>
    </view>
</view>