<view wx:if="{{ active }}" class="reader re-api virtual-list-container">
    <scroll-view class="vl-list-scroll" scroll-y bindscroll="onVlScroll" bindtouchstart="onVlScroll" bindtouchend="onVlTouchEnd" bindtouchmove="onVlTouchMove" bindtouchcancel="onVlTouchCancel" bindscrolltoupper="onVlScrollToUpper" bindscrolltolower="onVlScrollToLower">
    <view class="vl-list-placeholder is-start" style="height:{{ $vlStartPlaceholderHeight }}px;"></view>
        <view class="vl-list">
            <view wx:for="{{ $vlShowList }}" wx:for-item="$vlItem" wx:for-index="$vlItemIndex" wx:key="id" wx:if="{{ $vlStatus[$vlItem.id] !== 'recovery'}}" class="vl-item-container status-{{ $vlStatus[$vlItem.id] || 'none' }}" style="height:{{ $vlItem.$vlHeight || $vlItemHeight || 0 }}px;">
    <fc-api-renderer wx:if="{{ $vlStatus[$vlItem.id] !== 'weak' }}" data="{{ $vlItem }}" parentCid="{{ $cid }}" parentTid="{{ $tid }}" data-id="{{ $vlItem.id }}">
    </fc-api-renderer>
    </view>
</view>
<view class="mp-list-placeholder is-end" style="height:{{ $vlEndPlaceholderHeight }}px;"></view>
</scroll-view>

</view>
